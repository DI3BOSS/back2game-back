const endpoints = {
  games: "/games",
  users: "/users",
  login: "/login",
  root: "/",
  delete: "/delete",
  create: "/upload",
  idParam: "/:idGame",
  getById: "/gameById",
  gameId: "/:id",
};

export default endpoints;
